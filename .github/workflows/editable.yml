name: manylinux build

on: [push, pull_request]

jobs:
  build:
    runs-on: [ubuntu-latest]

    steps:

      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Build
        run:  ./tools/inplace-build.bash
